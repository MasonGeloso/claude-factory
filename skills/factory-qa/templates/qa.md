# Factory QA Checklist — <PROJECT NAME>

> The project-specific half of the `factory-qa` gate. QA always runs — this file only adds the checks
> that are specific to this codebase. Each bullet becomes its own todo and its own row in the QA
> report table, cited with what was actually looked at.
>
> This file holds only what is visible on the **running thing**. Anything decidable from a diff belongs
> in `factory/code-review.md`; anything decidable from a written plan belongs in
> `factory/plan-review.md`. Keep it under ~25 bullets — a checklist too long to walk item-by-item is
> the same as no checklist.

## Checklist
- <e.g. "Every new surface animates, and the motion derives from the values on screen — a uniform fade
  on everything is the finding.">
- <e.g. "Long names and large numbers do not push the page sideways at 390px.">
- <add more>

## Notes
<Known false positives, surfaces exempt from a rule and why, where the app's real pages live so a
reviewer can compare against them.>

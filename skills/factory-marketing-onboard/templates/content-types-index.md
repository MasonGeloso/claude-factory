# Factory Marketing Content Types — <PROJECT NAME>

> Index only — each content type's actual detail lives in its own file in this directory. Add a row
> here whenever a content type file is added, removed, or renamed; keep entries in the same order as
> `schedule.md` where a type is scheduled, on-demand types after.

| Content type | File | Cadence | Autonomy | One-line summary |
|---|---|---|---|---|
| <name> | [`<content-type-id>.md`](<content-type-id>.md) | `scheduled` \| `on-demand` | `autonomous-publish` \| `draft-only` | <one line> |

<!-- Add a row per content type file. -->

## Strategy / positioning (optional)

> If this project has an overall content strategy that cuts across every content type (why these
> types exist, in what priority, any account-wide positioning/voice rule) — put it here, not inside
> any one content type's file, since it isn't specific to one of them.
